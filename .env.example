# Copie este arquivo para .env e ajuste conforme necessário.
# .env NÃO deve ser commitado (já está no .gitignore).

# Banco de dados PostgreSQL
POSTGRES_DB=taskmanager
POSTGRES_USER=taskmanager
POSTGRES_PASSWORD=taskmanager

# App → Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=taskmanager
DB_USER=taskmanager
DB_PASSWORD=taskmanager

# Spring
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080
# Schema controlado por Flyway (db/migration/V*__*.sql). ddl-auto default é
# validate em todos os perfis — só sobrescreva se souber o que está fazendo.
DDL_AUTO=validate

# JWT — IMPORTANTE: trocar em produção por chave forte (mínimo 256 bits).
# O valor padrão é apenas para desenvolvimento.
JWT_SECRET=dGhpcy1pcy1hLWRldmVsb3BtZW50LWp3dC1zZWNyZXQta2V5LWRvLW5vdC11c2UtaW4tcHJvZHVjdGlvbi1wbGVhc2U=
