<?xml version="1.0" encoding="UTF-8"?>
<!-- Human-readable locally; Spring Boot native Logstash JSON in production. -->
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%-5level [%date{ISO8601}] [%thread] [%X{requestId}] %logger{36} - %kvp - %msg%n"/>

    <springProfile name="dev | default | test">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <springProperty name="CONSOLE_LOG_STRUCTURED_FORMAT"
                        source="logging.structured.format.console"
                        defaultValue="logstash"/>
        <include resource="org/springframework/boot/logging/logback/structured-console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
